import { BrowserRouter, Routes } from 'react-router-dom';

export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        {/* Screen routes registered here by /feature, e.g. <Route path="/" element={<IngestScreen />} /> */}
      </Routes>
    </BrowserRouter>
  );
}
